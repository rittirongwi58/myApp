import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { BmicalculatePage } from './bmicalculate';

@NgModule({
  declarations: [
    BmicalculatePage,
  ],
  imports: [
    IonicPageModule.forChild(BmicalculatePage),
  ],
})
export class BmicalculatePageModule {}
